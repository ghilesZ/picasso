(include_subdirs unqualified)

(library
  (public_name picasso)
  (name picasso)
  (libraries apron apronext (select canvas.ml from
                   (lablgtk2  -> backends/gtk/canvas.ok.ml)
                   (!lablgtk2 -> backends/gtk/canvas.ko.ml)))
)